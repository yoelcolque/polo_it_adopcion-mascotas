const Buscador = () => (
    <div>
        <input
            type="text"
            placeholder="Buscar por dirección..."
            className="w-full p-2 rounded border focus:outline-none focus:ring-2 focus:ring-primary"
        />
    </div>
);

export default Buscador;
